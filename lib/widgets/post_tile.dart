import 'package:flutter/material.dart';

class PostTile extends StatelessWidget {
  final String imageUrl;
  PostTile({this.imageUrl});
  Widget build(BuildContext context) {
    return Text('Hello World');
  }
}
